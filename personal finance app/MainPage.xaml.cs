namespace personal_finance_app;

public partial class MainPage : ContentPage
{
    public MainPage()
    {
        InitializeComponent();
        // BindingContext already set in XAML, so no need to do it again here
    }
}
